<nav class="navbar navbar-expand-md sticky-top bg-dark py-3" data-bs-theme="dark">
  <div class="container-fluid">
    <%= render partial: 'layouts/brand' %>
    <button data-bs-toggle="collapse" class="navbar-toggler" data-bs-target="#navcol-1">
      <span class="visually-hidden">Toggle navigation</span><span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navcol-1">
      <ul class="navbar-nav me-auto">
        <li class="nav-item">
          <%= link_to 'Categories', categories_path, class: 'nav-link ' + active_class(categories_path) %>
        </li>
        <li class="nav-item">
          <%= link_to 'Search', search_path, class: 'nav-link ' + active_class(search_path) %>
        </li>
        <% if @current_user.present? && @current_user.is_admin %>
          <li class="nav-item">
            <%= link_to 'Admin Panel', admin_path, class: 'nav-link ' + active_class(admin_path) %>
          </li>
        <% end %>
      </ul>
      <div class="d-md-none my-2">
        <div class="d-md-none my-2">
          <p class="text-white my-auto">
            Hi <%= get_name %>!
            <span class="float-end">
              <% if @current_user.present? %>
                <%= link_to 'User settings', settings_path %>&nbsp;|&nbsp;<%= link_to 'Logout', logout_path %>
              <% else %>
                <%= link_to 'Login', login_path %>&nbsp;|&nbsp;<%= link_to 'Register', register_path %>
              <% end %>
            </span>
          </p>
        </div>
      </div>
    </div>
    <div class="d-none d-md-block">
      <p class="text-white my-auto">
        Hi <%= get_name %>!&nbsp;
        <span>
          <% if @current_user.present? %>
            <%= link_to 'User settings', settings_path %>&nbsp;|&nbsp;<%= link_to 'Logout', logout_path %>
          <% else %>
            <%= link_to 'Login', login_path %>&nbsp;|&nbsp;<%= link_to 'Register', register_path %>
          <% end %>
        </span>
      </p>
    </div>
  </div>
</nav>